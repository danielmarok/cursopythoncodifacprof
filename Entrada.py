entrada = input ('Intgresa tu nombre completoCambio 1 para git: ')# Str input siempre devuelve string
print (entrada)
print(type(entrada))

#para cambiar la entrada uso el comando int() , int(entrada) me lo pasa a entero
#tambien esta la funcion float(), mismo uso de int
